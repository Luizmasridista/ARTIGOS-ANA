# Delegação — techlead — ADR-005 PWA iPad (2026-08-29)

CONTEXT SWITCH -> techlead

ROLE:
Act as techlead de Artigos Ana, especialista em PWA + Vite + Workbox + Safari iPadOS. Priorize boring tech e YAGNI: escolha simples, sustentável, sem dependência pesada, compatível com stack atual React 19 + Vite 8 + Go 1.26 stdlib + Electron (web vs desktop). Decida com evidência lendo os arquivos reais antes de escrever.

TASK:
Escreva ADR-005 e contrato PWA bloqueante para frontend/backend. Entregáveis: `docs/adr/ADR-005.md` (decisão vite-plugin-pwa vs vanilla + estratégia cache + critérios Lighthouse) e contrato atualizado em `docs/api.md` (ou `docs/contrato-pwa.md` se preferir separar) com shape `manifest.json`, rotas `sw.js` e API `registerSW.ts`. NÃO codar PWA agora, só decidir e especificar.

CONTEXT:
- Plano: `docs/planos/plano-2026-08-29-pwa-ipad.md` — goal PWA instalável iPad, site https rápido com offline cache, §4 arquitetura, §5 modelo Cache Storage, §6 tarefas, §8 aceitação, §9 riscos, §10 decisões fechadas (Vite+vite-plugin-pwa, Cloudflare Pages)
- Stack atual: `app/vite.config.mts:28` (defineConfig base './', plugins [react(), cspPlugin], manualChunks), `app/index.html:1` (viewport-fit=cover + apple-mobile-web-app-capable yes + theme #f7f7f5 + favicon), `app/src/main.tsx:1` (React 19, iPad detection via `useIpad.ts`, data-device="ipad", sem SW hoje), `app/src/styles/README.md:1` (CSS modular 18 files), `backend/internal/app/helpers.go:181` (securityHeaders com HSTS/CSP/no-store para /api/), `backend/internal/app/routes.go:10` (ServeMux, /api/health público, WWWDir serve dist via -www com Cache-Control immutable para /assets/), `app/package.json:8` (scripts build/test, sem vite-plugin-pwa ainda)
- ADRs referência: `docs/adr/ADR-004.md` (sessão Ana + hardening), `docs/adr/ADR-001.md`, `docs/adr/ADR-002.md`, `docs/adr/ADR-003.md` — seguir formato: Status aceito | Data | Autor | Contexto | Leitura (arquivos verificados) | Decisão | Consequências | Alternativas | Validação
- Constraints PWA: `manifest start_url "/" display standalone theme #f7f7f5`, offline precache `dist/assets`, runtime `StaleWhileRevalidate` para `GET /api/artigos*` e `GET /api/artigos/*/paginas/*/camada` (max 50 entradas, 7 dias), `NetworkOnly` para POST/PATCH/DELETE, `registerSW` só quando `!window.artigosAna` (web, não Electron), iPad Cache Storage ~50MB
- CORS/GROK: `backend/internal/app/helpers.go:82` (allowedOrigins com trycloudflare wildcard, strict CORS), deploy fixo Pages/Vercel não GROK efêmero (§9 risco)

REASONING:
- Leia TODOS os arquivos listados em CONTEXT antes de decidir; cite linhas (ex: helpers.go:181, vite.config.mts:28) no ADR §Leitura como ADR-004 fez
- Decida vite-plugin-pwa (Workbox) vs vanilla SW: justifique com trade-offs (DX Vite, precache automático dist/assets via glob, runtimeCaching declarativo, autoUpdate vs controle total vanilla; YAGNI: PWA v1 não precisa offline total sem servidor, só artigos já abertos)
- Defina estratégia cache precisa: `precache` (index.html + assets.* com revision), `runtime` (StaleWhileRevalidate GET /api/artigos*, NetworkOnly mutações, fallback offline.html opcional), expiration 50/7d, respeitar `Cache-Control: no-store` do backend para POST
- Defina critérios Lighthouse mensuráveis: PWA ≥90, manifest válido, sw.js registrado, start_url "/", icons 192/512, theme #f7f7f5, offline artigo já aberto funciona, sem regressão npm test 126 PASS + go vet PASS
- Siga single responsibility: ADR em `docs/adr/ADR-005.md` é único escritor deste arquivo; contrato em `docs/api.md` ou separado — não tocar em `app/vite.config.mts` nem `helpers.go` (isso é frontend/backend)
- Boring tech: preferir vite-plugin-pwa Workbox já ecossistema Vite; evitar WASM/.ipa/push (fora escopo §2)

STOP CONDITIONS:
- [ ] `docs/adr/ADR-005.md` existe com seções Contexto/Leitura/Decisão/Consequências/Alternativas/Validação, cita arquivos verificados com :linha, justifica vite-plugin-pwa vs vanilla vs WASM vs .ipa com prós/contras
- [ ] ADR define: libs (vite-plugin-pwa versão compatível Vite 8), workbox config (globPatterns, runtimeCaching com urlPattern GET /api/artigos*, handler StaleWhileRevalidate, expiration 50/7d, NetworkOnly para POST/PATCH/DELETE), manifest shape (name, start_url "/", display standalone, theme_color #f7f7f5, icons 192/512 + apple-touch-icon), critérios Lighthouse 90+
- [ ] Contrato escrito: `manifest.json` shape JSON exato, `sw.js` rotas e estratégias, `registerSW.ts` API (export function registerSW(): void, só em web !window.artigosAna, autoUpdate), headers esperados (`Cache-Control: public max-age=3600` para manifest/sw/assets, `no-store` para /api)
- [ ] Alternativas rejeitadas documentadas: vanilla SW, WASM/IndexedDB offline total, .ipa nativo, push notifications (fora)
- [ ] DO NOT need: implementar PWA code (VitePWA, registerSW, icons), só especificar; NÃO commitar; deixar na working tree
- [ ] MUST NOT: tocar em `app/vite.config.mts`, `app/src/main.tsx`, `backend/internal/app/helpers.go` (single writer violation)

OUTPUT:
- Arquivo `docs/adr/ADR-005.md` (formato ADR-004, ~80-120 linhas, sem travessão "—" com espaços, use vírgula/ponto, português correto)
- Contrato em `docs/api.md` atualizado (ou novo `docs/contrato-pwa.md` referenciado no ADR) com snippets JSON/TS exatos para frontend/backend consumirem sem ambiguidade
- Validação: lista de comandos no ADR §Validação (ex: `npm run build` deve gerar precache, Lighthouse PWA 90+)
- Report curto PT-BR: decisão tomada, porquê vite-plugin-pwa, contrato onde está, próximo passo frontend/backend paralelo
- Entrega na working tree, SEM commit, SEM co-author, SEM trailer de agente

INSTRUÇÕES INVIOLÁVEIS NA ENTREGA:
NÃO commitar; deixar na working tree; git-ops versiona depois. SEM co-author, SEM trailer de agente, SEM atribuição de IA. Single writer: você é único escritor de `docs/adr/ADR-005.md` e contrato.
